<form action="/todos/{{todo._id}}/edit" method="post">
  <input type="checkbox" name="done" {{#if todo.done}}checked{{/if}}>
  <input type="text" name="name" placeholoder="name" value="{{todo.name}}">
  <button type="submit">Edit</button>
</form>